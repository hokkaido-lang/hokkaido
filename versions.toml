# Single source of truth for all component versions.
# Run: ./scripts/bump-version.sh <component> <version>
# Or:  ./scripts/bump-version.sh --apply  (reads this file and updates all files)

[hokkaido]
version = "0.20.4"

[otaru]
version = "0.7.1"

[sapporo]
version = "0.3.3"

[cubical]
version = "0.3.0"
