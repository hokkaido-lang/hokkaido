package main

extern fn printf(fmt: string, ...) -> int

import math "util"

fn main() -> int {
    let x: int = math::add(10, 20)
    printf("math::add(10, 20) = %ld\n", x)  // expect 30
    return 0
}
