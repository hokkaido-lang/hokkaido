package util

pub fn add(a: int, b: int) -> int {
    return a + b
}

pub fn multiply(a: int, b: int) -> int {
    return a * b
}

// private function — not accessible from other packages
fn helper(x: int) -> int {
    return x * 2
}
